[build]
  publish = "public"
  functions = "api"
  environment = { CHROME = "local" }
  command = "#"

[[plugins]]
  package = "@netlify/plugin-functions-install-core"

[context.production]
  environment = { CHROME = "aws" }